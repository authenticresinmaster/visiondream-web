<!-- Codex 코드리뷰 | repo: C:\Users\노하우셀러\vision-claude\visiondream-web | 생성: 2026-07-11 07:51 -->

[변경 요약]
 lib/posts.ts | 10 ++++++++--
 1 file changed, 8 insertions(+), 2 deletions(-)

검토 결과, 이 diff 기준으로는 **발견된 문제 없음**입니다.

보안 관점에서도 변경 범위가 [lib/posts.ts](/mnt/c/Users/노하우셀러/vision-claude/visiondream-web/lib/posts.ts:43)의 정적 블로그 글 import/배열 등록에 한정되어 있고, 인증/인가, API route, DB 접근, 사용자 입력 처리, 관리자 기능을 건드리지 않습니다. 따라서 Claude가 RBAC, IDOR, 관리자 API, DB 누출 쪽을 지적했다면 이 변경분에 대해서는 **거짓양성**으로 보는 게 맞습니다.

확인한 것:
- 새 글 3개 `ko/en/ja` export 이름과 import alias 매칭 정상
- slug 중복 없음: `notion-goal-planner`, `career-vision-board`, `habit-tracker`
- cover 경로와 `public/blog/*.svg` 존재 확인
- `getAllPosts(lang)` 기반 목록/상세/사이트맵/RSS 흐름과 호환
- `tsc --noEmit --incremental false` 통과

메모: 일반 `tsc --noEmit`는 `tsconfig.tsbuildinfo`를 쓰려다 현재 읽기 전용 샌드박스에서 막혔지만, 증분 생성을 끄면 타입 검사는 통과했습니다. 코드 수정 필요 없음.