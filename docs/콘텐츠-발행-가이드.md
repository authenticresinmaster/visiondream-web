# 비전드림 — 콘텐츠 발행 가이드 (색인·AEO·영상)

> 공통 표준: `D:\키워드엔진\docs\콘텐츠 발행 표준.md` (**먼저 읽을 것**)
> 이 문서는 비전드림에만 해당하는 실측 상태와 할 일이다.

## 현재 상태 (2026-07-12 실측)

| 항목 | 값 |
|---|---|
| 발행(자사웹) | 48건 |
| **색인률** | **55%** → 60% 미만 = **발행 제동**(주 2편 상한) |
| 색인 실패 | 발견-미크롤 16건 · 구글모름 10건 · **크롤 후 색인거부 3건** |
| 기술 기반 | robots AI봇 16종 허용 · sitemap · JSON-LD(Article·FAQPage·VideoObject) · llms.txt · 다국어(ko/en/ja) — **양호** |
| 유튜브 Shorts | 45편 / 누적 1,186회 / **27편(60%) 조회 0** |

## 웹 — 지금 해야 할 일

1. **크롤 후 색인거부 3건**(품질 탈락) — 본문 2,000자 이상으로 보강하거나 병합:
   - `/blog/goal-management` · `/blog/okr-goals` · `/blog/reading-for-action`
   - ※ 이 셋은 **같은 슬러그의 영상이 있다** → 영상 임베드 + 대본 노출만으로도 본문이 두꺼워진다(아래 참조).
2. **발견-미크롤 16건** — 내부링크로 중요도 신호. 신규 발행보다 이게 우선.
3. 보강 후 GSC 재크롤 요청.

## 영상 ↔ 글 상호 임베드 (구현 완료, 미배포)

- `lib/post-video.ts` — 글 slug ↔ 영상 slug 매칭(정확일치 13쌍 + 별칭 21쌍 = **34개 글**에 영상 연결).
- 블로그 상세: 영상 임베드 + **대본 전문 텍스트** + `VideoObject` JSON-LD(대본 포함).
- 영상 상세: 같은 주제 글로 가는 역방향 링크(글이 실제 존재할 때만).
- **왜**: 임베드가 0건이라 두 자산이 서로를 전혀 돕지 않았다. 이 조치 하나로 **얇은 글은 두꺼워지고(색인↑) 영상은 피드 의존을 벗어나 검색 유입을 얻는다.**
- 상태: 타입체크·빌드 통과. **배포 대기** — 레포에 예약발행(publishedAt) 관련 미커밋 변경이 섞여 있으니 그 작업과 분리해 커밋할 것.

## 유튜브 Shorts — 제작 규칙 (다음 영상부터)

실측: 조회의 **96%가 Shorts 피드**(검색 2%). 피드 노출은 **시청지속률**이 결정한다.

| 항목 | 현재 | 규칙 |
|---|---|---|
| 발행 리듬 | 하루 30편 투하 → 23편 조회 0 | **주 3~5편** (7/3의 15편 배치는 4편만 0회, 총 982회) |
| 길이 | 50~60초 | **20~35초** (지속률 28% → 50% 목표) |
| 언어 | ko·en·en-US·ja 혼재 | **en-US 9편은 총 6회 = 사실상 사망.** 한국어·일본어 집중 또는 언어별 채널 분리 |
| 승자 패턴 | laziness(지속 44%) · focus(42%) | 이 훅·구성을 재현 (엔진이 학습해 다음 주제에 반영) |

**자막은 손댈 필요 없다** — 48편 전부 자동자막(ASR)이 정상 생성돼 있고 언어 인식도 정확하다.
(Data API의 `caption` 플래그는 *업로드* 자막만 세므로 false로 보이는 것뿐. 이 착각으로 불필요한 작업을 할 뻔했다.)

## 확인 명령

```bash
node D:\키워드엔진\index_audit.js --brand visiondream
node D:\키워드엔진\reinforce.js     # 색인 실패 원인 + 영상 WIN/LOSE 학습 결과
```
